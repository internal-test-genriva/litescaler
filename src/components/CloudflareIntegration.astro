<section class="support-section">
  <div class="content-wrapper">
    <div class="header">
      <h2>The smoothest, <span class="accent">fastest experience</span> on the web.</h2>
      <p>A perfect Google PageSpeed score for the smoothest experience on the web.</p>
    </div>

    <div class="grid-container">
      <div class="features">
        <div class="feature-item">
          <div class="icon">🚀</div>
          <div>
            <h3>LiteSpeed Enterprise Servers</h3>
            <p>Powered by NVMe Gen4 SSDs and advanced HTTP/3</p>
          </div>
        </div>

        <div class="feature-item">
          <div class="icon">⚡</div>
          <div>
            <h3>Server-Level Caching</h3>
            <p>LiteSpeed Cache (LSCache) is <span class="accent-text">built-in and optimized</span></p>
          </div>
        </div>

        <div class="checklist-area">
          <h3>What our performance stack achieves</h3>
          <ul class="checklist">
            <li><span class="check">✓</span> Insanely fast TTFB (Time to First Byte)</li>
            <li><span class="check">✓</span> Near-perfect Google Core Web Vitals</li>
            <li><span class="check">✓</span> Effortlessly handle massive traffic spikes</li>
            <li><span class="check">✓</span> Global content delivery with edge caching</li>
          </ul>
        </div>
      </div>

      <div class="chat-container">
        <img src="/PageSpeed_Insights.png" alt="PageSpeed Insights" style="width: 100%; height: auto; border-radius: 24px; display: block;" />
      </div>
    </div>

    <div class="footer-score">
      <span class="percentage">100/100</span>
      <span class="label">PageSpeed Score</span>
      <span class="source">Tested on Google PageSpeed Insights</span>
    </div>
  </div>
</section>

<style>
  /* --------------------------------------------------------------------------
     Singular Solidified Theme Audit
     Removing local LiteScaler overrides for global design system variables
     -------------------------------------------------------------------------- */

  .support-section {
    background-color: transparent; /* MATCHES THEME BACKGROUND */
    color: var(--color-text-primary);
    padding: 100px 24px;
    font-family: 'Inter', system-ui, -apple-system, sans-serif;
  }

  .content-wrapper {
    max-width: 1140px;
    margin: 0 auto;
  }

  .header {
    text-align: center;
    margin-bottom: 70px;
  }

  .header h2 {
    font-size: clamp(2rem, 5vw, 2.75rem);
    font-weight: 700;
    letter-spacing: -0.02em;
    margin-bottom: 16px;
  }

  .accent {
    color: var(--color-accent);
  }

  .accent-text {
    color: var(--color-accent);
    font-weight: 600;
  }

  .header p {
    color: var(--color-text-secondary);
    font-size: 1.1rem;
    max-width: 650px;
    margin: 0 auto;
  }

  .grid-container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: center;
  }

  /* Feature Styling */
  .feature-item {
    display: flex;
    gap: 20px;
    margin-bottom: 35px;
  }

  .icon {
    font-size: 1.5rem;
    opacity: 0.9;
  }

  .feature-item h3 {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 4px;
  }

  .feature-item p {
    color: var(--color-text-secondary);
  }

  .checklist-area h3 {
    font-size: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-text-secondary);
    margin: 40px 0 20px;
  }

  .checklist {
    list-style: none;
    padding: 0;
  }

  .checklist li {
    display: flex;
    gap: 12px;
    margin-bottom: 14px;
    color: var(--color-text-secondary);
    font-size: 0.95rem;
  }

  .check {
    color: var(--color-success);
    font-weight: 900;
  }

  /* Chat UI Mockup */
  .chat-container {
    padding: 0;
    border-radius: 0;
  }

  .chat-container img {
    width: 100%;
    height: auto;
    display: block;
  }

  .chat-box {
    display: flex;
    flex-direction: column;
    gap: 24px;
  }

  .user-row {
    display: flex;
    justify-content: flex-end;
    align-items: flex-end;
    gap: 12px;
  }

  .user-bubble {
    background: var(--color-bg-secondary);
    padding: 16px 20px;
    border-radius: 20px 20px 2px 20px;
    font-size: 0.9rem;
    border: 1px solid var(--color-border);
  }

  .support-row {
    display: flex;
    align-items: flex-start;
    gap: 12px;
  }

  .support-bubble {
    background: var(--color-info);
    padding: 20px;
    border-radius: 2px 20px 20px 20px;
    flex: 1;
    color: #fff;
    box-shadow: 0 15px 30px rgba(37, 99, 235, 0.25);
  }

  .bubble-header {
    display: flex;
    justify-content: space-between;
    margin-bottom: 8px;
    font-size: 0.75rem;
    font-weight: 600;
    opacity: 0.9;
  }

  .badge {
    background: rgba(255, 255, 255, 0.2);
    padding: 2px 8px;
    border-radius: 4px;
    text-transform: uppercase;
  }

  .avatar-sm {
    width: 36px;
    height: 36px;
    background: #334155;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .avatar-wrapper {
    position: relative;
  }

  .online-dot {
    position: absolute;
    bottom: 0;
    right: 0;
    width: 10px;
    height: 10px;
    background: var(--color-success);
    border: 2px solid var(--color-bg-primary);
    border-radius: 50%;
  }

  .typing-indicator {
    margin-left: 48px;
    color: var(--color-text-muted);
    font-style: italic;
    font-size: 0.85rem;
    background: var(--color-bg-tertiary);
    padding: 8px 16px;
    border-radius: 10px;
    width: fit-content;
  }

  /* Happiness Footer */
  .footer-score {
    margin-top: 80px;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 15px;
    padding-top: 40px;
    border-top: 1px solid var(--color-border);
  }

  .percentage {
    font-size: 3rem;
    font-weight: 800;
    color: var(--color-success);
  }

  .label {
    font-weight: 700;
    font-size: 1.25rem;
  }

  .source {
    color: var(--ls-text-muted);
    font-size: 0.9rem;
  }

  /* Responsive */
  @media (max-width: 960px) {
    .grid-container {
      grid-template-columns: 1fr;
      gap: 50px;
    }
    .support-section {
      padding: 60px 20px;
    }
  }

</style>